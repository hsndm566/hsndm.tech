# Hero Video and Matching Interaction Verification

At the desktop viewport, the supplied 2.6 MB MP4 is rendered behind the hero copy and workflow ledger. Its dark left-side negative space preserves headline contrast, while the subject remains on the right behind the operational panels. The muted, looping, inline video uses `preload="metadata"` so it does not request more data than needed before playback.

At the 375px mobile viewport, the video element is not rendered; the existing static poster remains in place and the sticky campaign CTA remains visible. The hero content, CTAs, and workflow record fit cleanly without relying on the video asset.

Matched-role suggestions are now buttons with hover, focus-visible, selected, and pressed states. Selecting a result provides visible feedback indicating that it is being carried into the campaign brief.
