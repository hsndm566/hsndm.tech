/**
 * Replace this empty value with the permanent Manus storage URL after the approved MP4 is uploaded.
 * An empty value prevents any speculative video request and preserves the poster-only mobile experience.
 */
export const HERO_VIDEO_URL = "";
